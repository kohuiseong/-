package operator;

public class Operator4 {
    public static void main(String[] args) {
        int sum3 = 2 * 2 + 3 * 3;
        int sum4 = (2 * 2) + (3 * 3);
        System.out.println("sum3 = " + sum3);
        System.out.println("sum4 = " + sum4);

        // sum3, sum4는 결과 값이 같다
        // 이렇게 복잡한 경우에는 괄호를 넣어주는것이 명확하다. 가독성이 좋고 명확하기 때문에 
    }
}
